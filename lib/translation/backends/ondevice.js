/**
 * Placeholder for future on-device translation (e.g. WASM / browser ML).
 * Returns null so callers can fall back or show "not available".
 */
export async function translateOnDevice() {
  return null;
}
